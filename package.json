{
  "name": "open-pdf-studio-library",
  "version": "0.1.0",
  "description": "Worldwide symbol, stamp and template library for Open PDF Studio",
  "type": "module",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/OpenAEC-Foundation/open-pdf-studio-library.git"
  },
  "scripts": {
    "build-tools": "mkdir -p build && ${DYNLEX:-dynlex} scripts/validate.dl -o build/validate && ${DYNLEX:-dynlex} scripts/build_index.dl -o build/build-index && ${DYNLEX:-dynlex} scripts/build_readme_media.dl -o build/build-readme-media",
    "test": "npm run build-tools && node --test",
    "validate": "build/validate",
    "build-index": "build/build-index",
    "check-index": "build/build-index --check",
    "build-readme-media": "build/build-readme-media"
  }
}
